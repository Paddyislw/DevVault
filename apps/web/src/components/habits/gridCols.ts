// components/habits/gridCols.ts
//
// Shared column template for the habit grid — used by both the header row
// and every HabitRow so the Mon–Sun columns line up pixel-for-pixel.
export const HABIT_GRID_COLS =
  "grid grid-cols-[minmax(140px,1fr)_repeat(7,36px)_52px] items-stretch gap-x-2";
